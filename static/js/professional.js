const $=s=>document.querySelector(s);
const api=async(url,opt={})=>{const r=await fetch(url,{credentials:'same-origin',...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Request failed');return d};
let me=null,conversationId=null,history=[];
function toast(t){const x=document.createElement('div');x.className='toast';x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2800)}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function closeMobileMenu(){const s=$('#sidebar'),b=$('#mobileBackdrop'),m=$('#mobileMenu');s?.classList.remove('mobile-open');b?.classList.add('hidden');m?.setAttribute('aria-expanded','false')}
function toggleMobileMenu(){const s=$('#sidebar'),b=$('#mobileBackdrop'),m=$('#mobileMenu');const open=!s.classList.contains('mobile-open');s.classList.toggle('mobile-open',open);b.classList.toggle('hidden',!open);m.setAttribute('aria-expanded',String(open))}
function closeAddMenu(){const m=$('#addMenu');m?.classList.add('hidden');$('#addToggle')?.setAttribute('aria-expanded','false')}
function toggleAddMenu(){const m=$('#addMenu');const open=m.classList.contains('hidden');m.classList.toggle('hidden',!open);$('#addToggle').setAttribute('aria-expanded',String(open))}
function closeModal(id){$(id)?.classList.add('hidden')}
function addMessage(value,role){const row=document.createElement('div');row.className='message '+role;row.innerHTML=`<div class="avatar">${role==='user'?'You':'T'}</div><div class="bubble">${esc(value)}</div>`;$('#chat').appendChild(row);$('#hero')?.classList.add('hidden');requestAnimationFrame(()=>window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'}))}
function resetChat(){conversationId=null;history=[];$('#chat').innerHTML='';$('#hero')?.classList.remove('hidden');$('#input').focus();closeMobileMenu()}
async function send(value){if(!value)return;addMessage(value,'user');$('#input').value='';$('#input').style.height='auto';$('#send').disabled=true;try{const d=await api('/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:value,mode:'melimi',history,conversation_id:conversationId})});conversationId=d.conversation_id||conversationId;addMessage(d.reply||'No response.','assistant');history.push({role:'user',content:value},{role:'assistant',content:d.reply||''});loadHistory(false)}catch(e){toast(e.message)}finally{$('#send').disabled=false}}
async function loadMe(){try{me=await api('/auth/me');$('#auth').classList.add('hidden');$('#accountName').textContent=me.username;$('#accountRole').textContent=me.role.toUpperCase();$('#avatar').textContent=me.username.slice(0,1).toUpperCase();if(['admin','owner'].includes(me.role))$('#adminLink').classList.remove('hidden');loadHistory(false)}catch(e){$('#auth').classList.remove('hidden')}}
async function loadHistory(show=true){if(!me)return;try{const d=await api('/conversations');const list=d.conversations||[];$('#historyList').innerHTML=list.length?list.map(c=>`<button class="history-item" data-id="${esc(c.id||c.conversation_id)}"><b>${esc(c.title||c.name||'Conversation')}</b><span>${esc(c.updated_at||c.created_at||'')}</span></button>`).join(''):'<div class="empty-state">కతనాలు లేవు.</div>';document.querySelectorAll('.history-item').forEach(b=>b.onclick=()=>openConversation(b.dataset.id));if(show){$('#history').classList.remove('hidden');closeMobileMenu()}}catch(e){if(show)toast(e.message)}}
async function openConversation(id){try{const d=await api('/conversations/'+encodeURIComponent(id));conversationId=d.conversation_id;history=[];$('#chat').innerHTML='';(d.messages||[]).forEach(m=>{const role=m.role||m.sender||'assistant';addMessage(m.content||m.message||'',role==='user'?'user':'assistant');history.push({role:role==='user'?'user':'assistant',content:m.content||m.message||''})});closeModal('#history');$('#hero')?.classList.add('hidden');closeMobileMenu()}catch(e){toast(e.message)}}
function openSettings(){closeMobileMenu();closeAddMenu();$('#settings').classList.remove('hidden');loadSettings()};function closeSettings(){closeModal('#settings')}
async function loadSettings(){try{const s=await api('/me/settings');$('#preferredMode').value=s.preferred_mode||'melimi';$('#responseLength').value=s.response_length||'normal';$('#memoryEnabled').checked=s.memory_enabled!==false}catch(e){toast(e.message)}}
async function saveSettings(){try{await api('/me/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({preferred_mode:$('#preferredMode').value,response_length:$('#responseLength').value,memory_enabled:$('#memoryEnabled').checked})});toast('Settings saved');closeSettings()}catch(e){toast(e.message)}}
function openProfile(){closeMobileMenu();closeAddMenu();if(!me){toast('Please sign in first.');return}$('#profileName').value=me.username;$('#profileEmail').value=me.email;$('#profileRole').value=me.role.toUpperCase();$('#profile').classList.remove('hidden')}
function openContent(){closeAddMenu();closeMobileMenu();$('#contentModal').classList.remove('hidden');$('#contentStatus').textContent=''}
function openWord(){closeAddMenu();closeMobileMenu();$('#wordModal').classList.remove('hidden');$('#wordStatus').textContent=''}
async function saveContent(){const file=$('#contentFile').files[0],body=$('#contentText').value.trim(),title=$('#contentTitle').value.trim();if(!file&&!body){$('#contentStatus').textContent='Content or a file is required.';return}$('#saveContent').disabled=true;try{if(file){const fd=new FormData();fd.append('file',file);await api('/melimi/content/upload',{method:'POST',body:fd})}else{await api('/melimi/content',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,content:body})})}toast('మేలిమి తెలిమి చేర్చబడింది');$('#contentText').value='';$('#contentTitle').value='';$('#contentFile').value='';closeModal('#contentModal')}catch(e){$('#contentStatus').textContent=e.message}finally{$('#saveContent').disabled=false}}
async function saveWord(){const word=$('#wordSource').value.trim(),melimi=$('#wordMelimi').value.trim(),meaning=$('#wordMeaning').value.trim();if(!word||!melimi){$('#wordStatus').textContent='Source word and Melimi word are required.';return}$('#saveWord').disabled=true;try{await api('/melimi/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({word,melimi_equivalent:melimi,meaning})});toast('పలుకు approval కోసం పంపబడింది');$('#wordSource').value='';$('#wordMelimi').value='';$('#wordMeaning').value='';closeModal('#wordModal')}catch(e){$('#wordStatus').textContent=e.message}finally{$('#saveWord').disabled=false}}
async function logout(){await fetch('/auth/logout',{method:'POST',credentials:'same-origin'});location.reload()}
$('#mobileMenu').onclick=toggleMobileMenu;$('#mobileBackdrop').onclick=closeMobileMenu;$('#addToggle').onclick=toggleAddMenu;$('#composerAdd').onclick=toggleAddMenu;$('#newChat').onclick=resetChat;$('#assistantLink').onclick=resetChat;$('#historyLink').onclick=()=>loadHistory(true);$('#profileLink').onclick=openProfile;$('#settingsLink').onclick=openSettings;$('#topSettings').onclick=openSettings;$('#adminLink').onclick=()=>{closeMobileMenu();location.href='/admin'};$('#contentEntry').onclick=openContent;$('#wordEntry').onclick=openWord;$('#send').onclick=()=>send($('#input').value.trim());$('#input').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send(e.target.value.trim())}else if(e.key==='Escape'){closeAddMenu()}});$('#input').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(e.target.scrollHeight,150)+'px'});document.querySelectorAll('.suggestion').forEach(x=>x.onclick=()=>send(x.dataset.message));$('#closeSettings').onclick=closeSettings;$('#closeProfile').onclick=()=>closeModal('#profile');$('#closeHistory').onclick=()=>closeModal('#history');$('#closeContent').onclick=()=>closeModal('#contentModal');$('#cancelContent').onclick=()=>closeModal('#contentModal');$('#closeWord').onclick=()=>closeModal('#wordModal');$('#cancelWord').onclick=()=>closeModal('#wordModal');$('#saveSettings').onclick=saveSettings;$('#saveContent').onclick=saveContent;$('#saveWord').onclick=saveWord;$('#logout').onclick=logout;$('#register').onchange=e=>{$('#authUserWrap').classList.toggle('hidden',!e.target.checked);$('#authSubmit').textContent=e.target.checked?'Create account':'చేరిక'};$('#authForm').onsubmit=async e=>{e.preventDefault();try{const register=$('#register').checked;const body=register?{username:$('#authUser').value,email:$('#authEmail').value,password:$('#authPass').value}:{identifier:$('#authEmail').value,password:$('#authPass').value};await api(register?'/auth/register':'/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});location.reload()}catch(x){$('#authError').textContent=x.message}};document.addEventListener('click',e=>{if(!e.target.closest('.add-wrap')&&!e.target.closest('#composerAdd'))closeAddMenu();if(e.target.classList.contains('modal'))e.target.classList.add('hidden')});loadMe();