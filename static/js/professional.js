const $=s=>document.querySelector(s);
const api=async(url,opt={})=>{const r=await fetch(url,{credentials:'same-origin',...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Request failed');return d};
let me=null,conversationId=null,history=[];
const uiKey='teluai_ui_settings';
function toast(t){const x=document.createElement('div');x.className='toast';x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2800)}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function closeMobileMenu(){const s=$('#sidebar'),b=$('#mobileBackdrop'),m=$('#mobileMenu');s?.classList.remove('mobile-open');b?.classList.add('hidden');m?.setAttribute('aria-expanded','false')}
function toggleMobileMenu(){const s=$('#sidebar'),b=$('#mobileBackdrop'),m=$('#mobileMenu');const open=!s.classList.contains('mobile-open');s.classList.toggle('mobile-open',open);b.classList.toggle('hidden',!open);m.setAttribute('aria-expanded',String(open))}
function closeAccountMenu(){const m=$('#accountMenu');m?.classList.add('hidden');$('#profileLink')?.setAttribute('aria-expanded','false')}
function toggleAccountMenu(e){e?.stopPropagation();const m=$('#accountMenu');const open=m.classList.contains('hidden');m.classList.toggle('hidden',!open);$('#profileLink').setAttribute('aria-expanded',String(open))}
function closeModal(id){$(id)?.classList.add('hidden')}
function addMessage(value,role){const row=document.createElement('div');row.className='message '+role;row.innerHTML=`<div class="avatar">${role==='user'?'You':'T'}</div><div class="bubble">${esc(value)}</div>`;$('#chat').appendChild(row);$('#hero')?.classList.add('hidden');requestAnimationFrame(()=>window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'}))}
function resetChat(){conversationId=null;history=[];$('#chat').innerHTML='';$('#hero')?.classList.remove('hidden');$('#input').focus();closeMobileMenu()}
async function send(value){if(!value)return;addMessage(value,'user');$('#input').value='';$('#input').style.height='auto';$('#send').disabled=true;try{const d=await api('/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:value,mode:'melimi',history,conversation_id:conversationId})});conversationId=d.conversation_id||conversationId;addMessage(d.reply||'No response.','assistant');history.push({role:'user',content:value},{role:'assistant',content:d.reply||''});loadHistory(false)}catch(e){toast(e.message)}finally{$('#send').disabled=false}}
async function loadMe(){try{me=await api('/auth/me');$('#auth').classList.add('hidden');$('#accountName').textContent=me.username;$('#accountRole').textContent=me.role.toUpperCase();$('#avatar').textContent=me.username.slice(0,1).toUpperCase();if(['admin','owner'].includes(me.role))$('#adminLink').classList.remove('hidden');loadHistory(false)}catch(e){$('#auth').classList.remove('hidden')}}
async function loadHistory(show=true){if(!me)return;try{const d=await api('/conversations');const list=d.conversations||[];$('#historyList').innerHTML=list.length?list.map(c=>`<button class="history-item" data-id="${esc(c.id||c.conversation_id)}"><b>${esc(c.title||c.name||'Conversation')}</b><span>${esc(c.updated_at||c.created_at||'')}</span></button>`).join(''):'<div class="empty-state">కతనాలు లేవు.</div>';document.querySelectorAll('.history-item').forEach(b=>b.onclick=()=>openConversation(b.dataset.id));if(show){$('#history').classList.remove('hidden');closeMobileMenu()}}catch(e){if(show)toast(e.message)}}
async function openConversation(id){try{const d=await api('/conversations/'+encodeURIComponent(id));conversationId=d.conversation_id;history=[];$('#chat').innerHTML='';(d.messages||[]).forEach(m=>{const role=m.role||m.sender||'assistant';addMessage(m.content||m.message||'',role==='user'?'user':'assistant');history.push({role:role==='user'?'user':'assistant',content:m.content||m.message||''})});closeModal('#history');$('#hero')?.classList.add('hidden');closeMobileMenu()}catch(e){toast(e.message)}}
function readUI(){try{return JSON.parse(localStorage.getItem(uiKey)||'{}')}catch{return {}}}
function effectiveTheme(theme){return theme==='system'?(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark'):theme}
function applyDisplay(){const s=readUI();document.documentElement.dataset.theme=effectiveTheme(s.theme||'dark');document.body.dataset.density=s.density||'normal';document.body.dataset.fontSize=s.fontSize||'normal'}
function applyTheme(theme){const s={...readUI(),theme};localStorage.setItem(uiKey,JSON.stringify(s));document.querySelectorAll('[data-theme]').forEach(b=>b.classList.toggle('active',b.dataset.theme===theme));applyDisplay()}
function openSettings(){closeMobileMenu();closeAccountMenu();$('#settings').classList.remove('hidden');loadSettings()}
async function loadSettings(){const local=readUI();applyDisplay();document.querySelectorAll('[data-theme]').forEach(b=>b.classList.toggle('active',b.dataset.theme===(local.theme||'dark')));$('#fontSize').value=local.fontSize||'normal';$('#density').value=local.density||'normal';$('#enterToSend').checked=local.enterToSend!==false;$('#confirmClear').checked=local.confirmClear!==false;try{const s=await api('/me/settings');$('#preferredMode').value=s.preferred_mode||'melimi';$('#responseLength').value=s.response_length||'normal';$('#memoryEnabled').checked=s.memory_enabled!==false}catch(e){if(me)$('#settingsStatus').textContent=e.message}}
async function saveSettings(){const selected=document.querySelector('[data-theme].active');const local={...readUI(),theme:selected?.dataset.theme||'dark',fontSize:$('#fontSize').value,density:$('#density').value,enterToSend:$('#enterToSend').checked,confirmClear:$('#confirmClear').checked};localStorage.setItem(uiKey,JSON.stringify(local));applyDisplay();try{await api('/me/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({preferred_mode:$('#preferredMode').value,response_length:$('#responseLength').value,memory_enabled:$('#memoryEnabled').checked})});toast('Settings saved');closeModal('#settings')}catch(e){$('#settingsStatus').textContent=e.message}}
function resetSettings(){localStorage.removeItem(uiKey);applyDisplay();loadSettings();toast('Settings reset')}
function openProfile(){closeMobileMenu();closeAccountMenu();if(!me){toast('Please sign in first.');return}$('#profileName').value=me.username;$('#profileEmail').value=me.email;$('#profileRole').value=me.role.toUpperCase();$('#profile').classList.remove('hidden')}
async function logout(){await fetch('/auth/logout',{method:'POST',credentials:'same-origin'});location.reload()}
$('#mobileMenu').onclick=toggleMobileMenu;$('#mobileBackdrop').onclick=closeMobileMenu;$('#newChat').onclick=resetChat;$('#assistantLink').onclick=resetChat;$('#historyLink').onclick=()=>loadHistory(true);$('#profileLink').onclick=toggleAccountMenu;$('#profileMenuItem').onclick=openProfile;$('#settingsLink').onclick=openSettings;$('#topSettings').onclick=openSettings;$('#adminLink').onclick=()=>{closeAccountMenu();closeMobileMenu();location.href='/admin'};$('#logout').onclick=logout;$('#send').onclick=()=>send($('#input').value.trim());$('#input').addEventListener('keydown',e=>{if(e.key==='Escape'){closeModal('#settings');closeAccountMenu()}if(e.key==='Enter'&&!e.shiftKey&&readUI().enterToSend!==false){e.preventDefault();send(e.target.value.trim())}});$('#input').addEventListener('input',e=>{e.target.style.height='auto';e.target.style.height=Math.min(e.target.scrollHeight,150)+'px'});document.querySelectorAll('.suggestion').forEach(x=>x.onclick=()=>send(x.dataset.message));$('#closeSettings').onclick=()=>closeModal('#settings');$('#closeProfile').onclick=()=>closeModal('#profile');$('#closeHistory').onclick=()=>closeModal('#history');$('#saveSettings').onclick=saveSettings;$('#resetSettings').onclick=resetSettings;document.querySelectorAll('[data-theme]').forEach(b=>b.onclick=()=>applyTheme(b.dataset.theme));window.matchMedia('(prefers-color-scheme: light)').addEventListener?.('change',()=>{if(readUI().theme==='system')applyDisplay()});$('#register').onchange=e=>{$('#authUserWrap').classList.toggle('hidden',!e.target.checked);$('#authSubmit').textContent=e.target.checked?'Create account':'చేరిక'};$('#authForm').onsubmit=async e=>{e.preventDefault();try{const register=$('#register').checked;const body=register?{username:$('#authUser').value,email:$('#authEmail').value,password:$('#authPass').value}:{identifier:$('#authEmail').value,password:$('#authPass').value};await api(register?'/auth/register':'/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});location.reload()}catch(x){$('#authError').textContent=x.message}};document.addEventListener('click',e=>{if(!e.target.closest('#profileLink')&&!e.target.closest('#accountMenu'))closeAccountMenu();if(e.target.classList.contains('modal'))e.target.classList.add('hidden')});applyDisplay();loadMe();