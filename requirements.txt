fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0

# PostgreSQL layer (learning candidates, approved knowledge, response
# cache, per-user memory). Optional at runtime: the app still runs fine on
# file/JSON storage alone if DATABASE_URL is unset.
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
